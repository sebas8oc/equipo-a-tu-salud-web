---
---

<section id="nosotros" class="section">
	<div class="container">
		<div class="about-content">
			<div class="about-text">
				<h2>¿Quiénes Somos?</h2>
				<p class="lead">
					Somos un equipo prestador de servicios en salud cuya finalidad es contribuir a mejorar la calidad de vida del individuo mediante acciones encaminadas a la prevención de enfermedades, a través de un grupo de profesionales idóneos brindando servicio humano, eficaz y oportuno.
				</p>
			</div>
			<div class="values-grid">
				<div class="value-item">
					<div class="value-header">
						<div class="icon">🎯</div>
						<h3>Misión</h3>
					</div>
					<p>Brindar un servicio eficiente y de calidad, ofreciendo seguridad y comodidad a nuestros clientes en cada servicio según sus necesidades.</p>
				</div>
				<div class="value-item">
					<div class="value-header">
						<div class="icon">🌟</div>
						<h3>Visión</h3>
					</div>
					<p>Convertirnos en una empresa reconocida en el país mediante un servicio excepcional.</p>
				</div>
				<div class="value-item full-width">
					<div class="value-header">
						<div class="icon">💎</div>
						<h3>Valores</h3>
					</div>
					<div class="values-list">
						<span class="value-tag">Seguridad</span>
						<span class="value-tag">Puntualidad</span>
						<span class="value-tag">Atención</span>
						<span class="value-tag">Honestidad</span>
						<span class="value-tag">Confianza</span>
						<span class="value-tag">Presentación</span>
					</div>
				</div>
			</div>
		</div>
	</div>
</section>

<style>
	.about-content {
		max-width: 1000px;
		margin: 0 auto;
	}

	.about-text {
		text-align: center;
		margin-bottom: 3rem;
	}

	.about-text h2 {
		color: var(--primary);
		margin-bottom: 1rem;
		font-size: 2.5rem;
	}

	.lead {
		font-size: 1.125rem;
		line-height: 1.8;
		color: var(--neutral-600);
		max-width: 800px;
		margin: 0 auto;
	}

	.values-grid {
		display: grid;
		grid-template-columns: 1fr 1fr;
		gap: 2rem;
		margin-top: 3rem;
	}

	.value-item {
		background: white;
		padding: 2rem;
		border-radius: 12px;
		box-shadow: 0 2px 8px rgba(0, 0, 0, 0.1);
		transition: all 0.3s ease;
	}

	.value-item:hover {
		transform: translateY(-4px);
		box-shadow: 0 8px 25px rgba(0, 0, 0, 0.15);
	}

	.full-width {
		grid-column: 1 / -1;
	}

	.value-header {
		display: flex;
		align-items: center;
		gap: 1rem;
		margin-bottom: 1rem;
	}

	.icon {
		font-size: 2rem;
		width: 3rem;
		height: 3rem;
		display: flex;
		align-items: center;
		justify-content: center;
		background: linear-gradient(135deg, var(--primary), var(--secondary));
		border-radius: 50%;
	}

	.value-item h3 {
		color: var(--primary);
		font-size: 1.25rem;
	}

	.value-item p {
		color: var(--neutral-600);
		line-height: 1.6;
	}

	.values-list {
		display: flex;
		flex-wrap: wrap;
		gap: 0.75rem;
	}

	.value-tag {
		background: linear-gradient(135deg, var(--primary), var(--secondary));
		color: white;
		padding: 0.5rem 1rem;
		border-radius: 20px;
		font-size: 0.875rem;
		font-weight: 500;
	}

	@media (max-width: 768px) {
		.values-grid {
			grid-template-columns: 1fr;
			gap: 1.5rem;
		}

		.about-text h2 {
			font-size: 2rem;
		}

		.value-item {
			padding: 1.5rem;
		}
	}
</style>